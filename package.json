{
  "name": "sipnato-pos",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm --filter @sipnato/web --filter @sipnato/server --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0"
  }
}
