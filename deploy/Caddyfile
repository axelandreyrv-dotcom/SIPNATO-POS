www.dosuxsoft.com {
    root * /srv

    # API REST → Fastify (servicio Docker "server")
    handle /api/* {
        reverse_proxy server:3000
    }

    # Auth endpoints — prefijo /auth, no /api
    handle /auth/* {
        reverse_proxy server:3000
    }

    # Health check
    handle /health {
        reverse_proxy server:3000
    }

    # SPA: servir archivos estáticos, fallback a index.html para React Router
    handle {
        try_files {path} /index.html
        file_server
    }

    # Security headers (CLAUDE.md §6.6)
    header {
        Strict-Transport-Security "max-age=63072000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"
        Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'"
        -Server
    }

    encode gzip zstd
}

# Redirigir dosuxsoft.com (sin www) → www.dosuxsoft.com
dosuxsoft.com {
    redir https://www.dosuxsoft.com{uri} permanent
}
